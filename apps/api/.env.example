# Copy to `apps/api/.env` or use repo-root `.env` when starting uvicorn from `apps/api`.
# Defaults below match docker-compose Postgres in the repo root README.

DATABASE_URL=postgresql+psycopg://tebekaye:tebekaye_dev@127.0.0.1:5432/tebekaye

# Local JWT signing (change for any shared/staging environment)
JWT_SECRET=local-dev-only-change-in-production

OPENAI_API_KEY=
