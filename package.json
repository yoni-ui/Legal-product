{
  "name": "tebekaye",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "cd apps/web && node ./node_modules/next/dist/bin/next dev",
    "dev:web": "cd apps/web && node ./node_modules/next/dist/bin/next dev",
    "build": "npm run build -w web",
    "lint": "npm run lint -w web",
    "typecheck": "npm run typecheck -w web"
  },
  "engines": {
    "node": ">=20"
  }
}
